spring:
  application:
    name: Authentication
  datasource:
     url: jdbc:postgresql://localhost:5432/SecurityCredentials
     username: postgres
     password: root
     driver-class-name: org.postgresql.Driver
