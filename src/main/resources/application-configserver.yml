spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Denis-Liubenkov/Config-server.git
          default-label: master
server:
  port: 8888